name: Swift

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build
        run: swift build
